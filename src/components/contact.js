import React from "react";

const Contact = (props) => {
  return (
    <div
      style={{
        fontSize: "2rem",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "500px",

        maxHeight: "1000px",
      }}
    >
      Contact Us Page
    </div>
  );
};

export default Contact;
